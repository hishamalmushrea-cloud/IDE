name: Build and test

on:
  push:
    branches: [ "main", "dev" ]
    paths-ignore:
      - '**.md'
      - 'fastlane/**'
      - '.github/workflows/crowdin_contributors.yml'
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'fastlane/**'
      - '.github/workflows/crowdin_contributors.yml'
  workflow_dispatch: { }

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  IDE_SIGNING_ALIAS: ${{ secrets.IDE_SIGNING_ALIAS }}
  IDE_SIGNING_AUTH_PASS: ${{ secrets.IDE_SIGNING_AUTH_PASS }}
  IDE_SIGNING_AUTH_USER: ${{ secrets.IDE_SIGNING_AUTH_USER }}
  IDE_SIGNING_KEY_PASS: ${{ secrets.IDE_SIGNING_KEY_PASS }}
  IDE_SIGNING_STORE_PASS: ${{ secrets.IDE_SIGNING_STORE_PASS }}
  IDE_SIGNING_URL: ${{ secrets.IDE_SIGNING_URL }}
  IDE_SIGNING_KEY_BIN: ${{ secrets.IDE_SIGNING_KEY_BIN }}
  BUILD_JAVA_VERSION: '17'
  BUILD_JAVA_DIST: 'temurin'
  BUILD_IS_RELEASE: ${{ github.ref == 'refs/heads/main' }}
  ReleaseAIDENext: ${{ github.ref == 'refs/heads/main' }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  build_apk:
    strategy:
      fail-fast: false
      matrix:
        build_type: ['debug', 'release']
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.BUILD_JAVA_VERSION }}
          distribution: ${{ env.BUILD_JAVA_DIST }}
      - name: Change Gradle wrapper permissions
        run: chmod +x ./gradlew
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Install latest cmdline-tools and Android SDK platform
        run: |
          export ANDROID_SDK_ROOT=$ANDROID_HOME
          echo "Pre-installed platforms:"
          ls -d $ANDROID_SDK_ROOT/platforms/*/ 2>/dev/null || echo "No platforms dir"
          echo "Pre-installed build-tools:"
          ls -d $ANDROID_SDK_ROOT/build-tools/*/ 2>/dev/null || echo "No build-tools dir"
          CMDLINE_TOOLS_ZIP=/tmp/cmdline-tools.zip
          CMDLINE_TOOLS_DIR=$ANDROID_SDK_ROOT/cmdline-tools/latest
          if [ ! -f "$CMDLINE_TOOLS_DIR/bin/sdkmanager" ]; then
            echo "Downloading latest cmdline-tools..."
            curl -fsSL -o "$CMDLINE_TOOLS_ZIP" \
              "https://dl.google.com/android/repository/commandlinetools-linux-12209519_latest.zip"
            mkdir -p "$CMDLINE_TOOLS_DIR"
            unzip -q "$CMDLINE_TOOLS_ZIP" -d /tmp/cmdline-tools-tmp
            mv /tmp/cmdline-tools-tmp/cmdline-tools/* "$CMDLINE_TOOLS_DIR/"
            rm -rf /tmp/cmdline-tools-tmp "$CMDLINE_TOOLS_ZIP"
          fi
          yes | $CMDLINE_TOOLS_DIR/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT \
            "platforms;android-34" "platforms;android-35" "build-tools;33.0.0" "build-tools;34.0.0" "build-tools;35.0.0" || true
          echo "Installed platforms:"
          ls -d $ANDROID_SDK_ROOT/platforms/*/ 2>/dev/null || echo "No platforms dir"
      - name: Assemble ${{ matrix.build_type }} APK
        run: |
          set -o pipefail
          build_type=${{ matrix.build_type }}
          ./gradlew :core:app:assemble${build_type^} --stacktrace 2>&1 | tee gradle-build-${build_type}.log
      - name: Report build failure to PR
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          build_type=${{ matrix.build_type }}
          echo "### Job Failure: Build APK (${build_type})" > /tmp/report.md
          echo '```' >> /tmp/report.md
          tail -n 80 gradle-build-${build_type}.log 2>/dev/null >> /tmp/report.md || true
          echo '```' >> /tmp/report.md
          gh pr comment 1 --repo hishamalmushrea-cloud/IDE --body-file /tmp/report.md || true
      - name: Copy ${{ matrix.build_type }} APK
        run: |
          find core/app/build/outputs/apk/ -type f -name "*.apk" -exec cp {} ./ \;
          ls -la *.apk
      - name: Upload universal ${{ matrix.build_type }} APK
        uses: actions/upload-artifact@v4
        with:
          name: apk-${{ matrix.build_type }}
          path: '*.apk'

  run_unit_tests:
    name: Run unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.BUILD_JAVA_VERSION }}
          distribution: ${{ env.BUILD_JAVA_DIST }}
      - name: Change Gradle wrapper permissions
        run: chmod +x ./gradlew
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Install Android SDK platform
        run: |
          export ANDROID_SDK_ROOT=$ANDROID_HOME
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \
            --sdk_root=$ANDROID_SDK_ROOT             "platforms;android-34" "platforms;android-35" "build-tools;33.0.0" "build-tools;34.0.0" "build-tools;35.0.0" || true
      - name: Run unit tests
        run: |
          set -o pipefail
          ./gradlew runTestsInCI --continue --stacktrace 2>&1 | tee unit-tests.log
        env:
          ANDROIDIDE_TEST: true
      - name: Report unit test failure to PR
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "### Job Failure: Run unit tests" > /tmp/report-tests.md
          echo '```' >> /tmp/report-tests.md
          tail -n 80 unit-tests.log 2>/dev/null >> /tmp/report-tests.md || true
          echo '```' >> /tmp/report-tests.md
          gh pr comment 1 --repo hishamalmushrea-cloud/IDE --body-file /tmp/report-tests.md || true
      - name: Upload test reports
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results-unit-tests
          path: '**/build/reports/tests/'

  run_tooling_api_tests:
    name: Build and test Tooling API
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.BUILD_JAVA_VERSION }}
          distribution: ${{ env.BUILD_JAVA_DIST }}
      - name: Change Gradle wrapper permissions
        run: chmod +x ./gradlew
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Install Android SDK platform
        run: |
          export ANDROID_SDK_ROOT=$ANDROID_HOME
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \
            --sdk_root=$ANDROID_SDK_ROOT             "platforms;android-34" "platforms;android-35" "build-tools;33.0.0" "build-tools;34.0.0" "build-tools;35.0.0" || true
      - name: Run tooling API tests
        run: |
          set -o pipefail
          ./gradlew :tooling:impl:build --continue --stacktrace 2>&1 | tee tooling-tests.log
        env:
          ANDROIDIDE_TEST: true
          TEST_TOOLING_API_IMPL: true
      - name: Report tooling API test failure to PR
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "### Job Failure: Tooling API tests" > /tmp/report-tooling.md
          echo '```' >> /tmp/report-tooling.md
          tail -n 80 tooling-tests.log 2>/dev/null >> /tmp/report-tooling.md || true
          echo '```' >> /tmp/report-tooling.md
          gh pr comment 1 --repo hishamalmushrea-cloud/IDE --body-file /tmp/report-tooling.md || true
      - name: Upload test reports
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-results-tooling-api-tests
          path: '**/build/reports/tests/'

  publish:
    name: Publish release
    runs-on: ubuntu-latest
    needs: [ build_apk, run_unit_tests, run_tooling_api_tests ]
    if: |
      always()
      && github.ref == 'refs/heads/main'
      && github.event_name != 'pull_request'
      && needs.build_apk.result == 'success'
      && needs.run_unit_tests.result == 'success'
      && needs.run_tooling_api_tests.result == 'success'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.BUILD_JAVA_VERSION }}
          distribution: ${{ env.BUILD_JAVA_DIST }}
      - name: Download APKs
        uses: actions/download-artifact@v4
        with:
          pattern: apk-release
          merge-multiple: true
      - name: Change Gradle wrapper permissions
        run: chmod +x ./gradlew
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Generate checksums
        run: |
          mkdir -p build
          md5sum *.apk > build/CHECKSUMS-md5.txt
          sha256sum *.apk > build/CHECKSUMS-sha256.txt
          sha512sum *.apk > build/CHECKSUMS-sha512.txt
          find build -type f -name "CHECKSUMS*.txt" -exec echo {} \; -exec cat {} \; -exec echo "" \;
      - name: Publish release
        run: ./gradlew nyxMake nyxMark nyxPublish publish release --stacktrace
      - name: Archive Nyx state file
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: .nyx-state-${{ github.job }}.json
          path: build/nyx-state.json
